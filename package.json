{
  "name": "affordiax",
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i ./input.css -o ./output.css --minify",
    "build": "npm run build:css && node build.js"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1",
    "wrangler": "^4.135.0"
  }
}